<% content_for :title, "Editing expense" %>

<h1>Editing expense</h1>

<%= render "form", expense: @expense %>

<br>

<div>
  <%= link_to "Show this expense", @expense %> |
  <%= link_to "Back to trip", trip_path(@expense.trip_id)%>
</div>
